<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_crazy_habits"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_crazy_habits"
        android:supportsRtl="true"
        android:theme="@style/Theme.Crazy_habits.Starting"
        android:name="com.example.crazy_habits.App">
        <activity
            android:name="com.example.crazy_habits.FirstActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>